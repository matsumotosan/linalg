#ifndef PPRINT_CPP
#define PPRINT_CPP

#include "matrix.h"
#include "pprint.h"

template<typename T>
void pprint(const int &mat) {
    unsigned rows = mat.get_rows();
    unsigned cols = mat.get_cols(); 
}

#endif
